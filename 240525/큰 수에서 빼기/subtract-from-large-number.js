const fs = require("fs");
let input = fs.readFileSync(0).toString().split(" ");

let a = Number(input[0]);
if (b === null) {
    let b = 0
} else {
    let b = Number(input[1]);
}


if (a > b) {
    console.log(a - b);
} else if (b > a) {
    console.log(b - a);
}